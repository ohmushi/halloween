import { Mystery.HttpExceptionFilter } from './mystery.http-exception.filter';

describe('Mystery.HttpExceptionFilter', () => {
  it('should be defined', () => {
    expect(new Mystery.HttpExceptionFilter()).toBeDefined();
  });
});
